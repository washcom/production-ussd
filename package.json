{
  "name": "ussd-prod",
  "version": "0.1.0",
  "description": "USSD gateway for browsing underwriters/products and requesting a policy purchase.",
  "main": "build/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node build/server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.13.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
